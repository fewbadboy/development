# Effective Dart

## Style

- 使用 `lowerCamelCase` 命名 `const`
- 对未使用的回调参数使用通配符(`_`)
- import 顺序 `dart:`, `package:`, 其他 import
- `export` 在所有 import 之后
- 插值组成字符串和值
- 集合使用 `isEmpty` `isNotEmpty` 判断空或非空
- `for-in` 遍历序列，`forEach` 每个元素上调用一些已经存在的函数
- `whereType()` 通过类型过滤集合
- 尽可能使用初始化形式
