# Glossary

## Attribute

是可扩展 HTML 或者 XML 元素，形式 `name="value"`

## 渲染引擎

- Blink

## Webkit

基于标记的正确格式化网页的框架

## Bun

基于 Apple 的 JavaScriptCore 构建

## 引擎

- JavaScript 引擎(解析并执行 JavaScript 程序)
  - V8
  - SpiderMonkey
  - JavaScriptCore
- 渲染引擎
  - Blink
  - WebKit
  - Gecko

## Fetch metadata request header

- Sec-Fetch-Site
- Sec-Fetch-Mode
- Sec-Fetch-User
- Sec-Fetch-Dest

上面的标头不能通过 JavaScript 修改

## Houdini

## Jank

用户界面卡顿的表现

## JSON

在对象上定义一个 `toJSON()` 示例方法，`JSON.stringify()` 调用对象实现自定义序列化

## Long Task

需要超过 50ms 完成的任务

## 无损压缩

gzip，WebP, PNG, Zstandard

## Parser 解析器

## Property

- CSS: 一种特征 `color: red;`
- JavaScript: 对象的成员

## Random

- `Crypto.getRandomValues()` 提供密码学安全的数字

## Window

### customElements

注册新的自定义元素

### localStorage

### sessionStorage

### requestAnimationFrame()

- 回调函数在下一次重绘之前调用

### postMessage()

## [DOM](https://developer.mozilla.org/zh-CN/docs/Web/API/Document_Object_Model)

### Document

- images: 文档中所有的图片
- lastModified
- forms

### Element

- classList
- clientHeight: 没定义 CSS 或内联布局元素 为 0；否则 CSS height + CSS padding - 水平滚动条高度(如果存在)
- shadowRoot
- slot
- attachShadow(options): 给元素挂载一个 Shadow DOM 并返回引用
- closest(selectors): 最近的祖先元素
- requestFullscreen()
- scrollIntoView(option)

### Event

- currentTarget: 事情处理器绑定的元素
- target: 事件处理器触发的元素
- preventDefault(): 阻止事件的默认行为(如表单提交刷新页面)
- stopPropagation(): 阻止捕获和冒泡阶段中当前事件的进一步传播

### EventTarget

- addEventListener
- dispatchEvent
- removeEventListener

### Node

种类型的 DOM API 对象会从这个接口继承

- childNodes
- firstChild/lastChild
- nextSibling
- nodeType/nodeValue
- parentNode/parentElement
- previousSibling
- textContent

### ShadowRoot

样式隔离、结构封装、组件可控性

与文档的主 DOM 树分开渲染 ShadowRoot

```js
// 为新元素创建一个类
class WordCount extends HTMLParagraphElement {
  constructor() {
    super();

    // 计数器指向元素的父级
    var wcParent = this.parentNode;

    function countWords(node) {
      var text = node.innerText || node.textContent;
      return text.trim().split(/\s+/g).length;
    }

    var count = "Words: " + countWords(wcParent);

    // 创建一个 shadow root
    var shadow = this.attachShadow({ mode: "open" });

    // 创建文本节点并向其添加计数器
    var text = document.createElement("span");
    text.textContent = count;

    shadow.appendChild(text);

    // 当元素内容发生变化时更新计数
    setInterval(function () {
      var count = "Words: " + countWords(wcParent);
      text.textContent = count;
    }, 200);
  }
}

// 定义新元素
customElements.define("word-count", WordCount, { extends: "p" });
```
