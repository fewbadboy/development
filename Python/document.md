# Python

## poetry / uv

依赖管理工具

## [虚拟环境](https://fastapi.tiangolo.com/zh/virtual-environments/#activate-the-virtual-environment)

```shell
# Windows PowerShell
# Python 自带的 venv 模块创建一个 env 目录的虚拟环境
python -m venv env
# 激活虚拟环境
env\Scripts\Activate.ps1
```

## 升级 pip

```shell
python -m pip install --upgrade pip
```
