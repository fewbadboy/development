# 正则表达式

```js
// 捕获元
// (pattern)  通过 $0... 匹配捕获元对应的结果

// 非捕获元

// (?:) 

// exp1(?=exp2) 查找exp2前的exp1

// (?<=exp2)exp1 查找exp2后的exp1

// exp1(?!=exp2) 查找后面不是exp2的exp1

// (?<!exp2)exp1 查找前面不是exp2的exp1

''.match(/^$/)
/java(?=script)/.test('javascript') // exec('javascript')
```
